# Base URL of the API, as seen from the browser. The dashboard calls the API
# directly over CORS (the API allow-lists http://localhost:3000), so this points
# at the host-exposed API port, not the docker-network service name.
NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1
